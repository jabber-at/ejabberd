--- a/ejabberdctl.cfg.example
+++ b/ejabberdctl.cfg.example
@@ -146,6 +146,17 @@
 #
 #EJABBERD_CONFIG_PATH=/etc/ejabberd/ejabberd.yml
 
+# EJABBERD_MONIT_INTEGRATION: ejabberd integration into monit
+#
+# Set to yes to enable integration into Monit[1]. Monit will watch ejabberd,
+# restart it if it crashes and sends you a mail on any status changes.
+#
+# [1] http://mmonit.com/monit/
+#
+# Default: no
+#
+#EJABBERD_MONIT_INTEGRATION=no
+
 #.
 #'
 # vim: foldmarker=#',#. foldmethod=marker:
--- a/ejabberd.init.template
+++ b/ejabberd.init.template
@@ -22,6 +22,7 @@
 CTL="$DIR"/ejabberdctl
 USER=ejabberd
 EJABBERDRUN=/var/run/ejabberd
+EJABBERD_MONIT_INTEGRATION=no
 
 test -x "$CTL" || {
 	echo "ERROR: ejabberd not found: $DIR"
@@ -53,11 +54,13 @@
       su - $USER -c "$CTL start"
       su - $USER -c "$CTL started"
     fi
+    [ "$EJABBERD_MONIT_INTEGRATION" != 'yes' ] || monit monitor ejabberd
     echo "done."
     ;;
   stop)
     test -x "$CTL" || exit 0
     echo "Stopping ejabberd..."
+    [ "$EJABBERD_MONIT_INTEGRATION" != 'yes' ] || monit unmonitor ejabberd
     if $CTL status >/dev/null || test $? = 1 ; then
       su - $USER -c "$CTL stop"
       su - $USER -c "$CTL stopped"
