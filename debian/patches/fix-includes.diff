--- a/rebar.config
+++ b/rebar.config
@@ -42,6 +42,8 @@
             {i, "include"},
 	    {i, "deps/fast_xml/include"},
 	    {i, "deps/xmpp/include"},
+	    {i, "deps/xmpp/include"},
+            {i, "/usr/lib/erlang/lib/p1_xmpp-1.1.16/include"},
             {if_var_false, debug, no_debug_info},
             {if_var_true, debug, debug_info},
             {if_var_true, roster_gateway_workaround, {d, 'ROSTER_GATWAY_WORKAROUND'}},
